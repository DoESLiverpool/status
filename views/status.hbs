        <div class="row">
            <div class="col s12 m6 offset-m3">
                <div class="card tooltipped" id="{{status/obj_name}}" data-position="bottom" data-delay="100" {{#if status/alttext}}{{#if status/offline}}data-tooltip="Broken Since: {{status/offline_date}}"{{else}}data-tooltip="Working Since: {{status/offline_date}}"{{/if}}{{else}}{{#if status/offline}}data-tooltip="Offline Since: {{status/offline_date}}"{{else}}data-tooltip="Online Since: {{status/offline_date}}"{{/if}}{{/if}}>
                    <div class="card-content">
                        <span class="card-title black-text">{{status/friendly-name}}</span>
                        {{#if status/alttext}}
                            {{#if status/offline}}
                                <h3 class="red-text">Broken</h3>
                            {{else}}
                                <h3 class="green-text">Working</h3>
                            {{/if}}
                            {{else}}
                            {{#if status/offline}}
                                <h3 class="red-text">Offline</h3>
                            {{else}}
                                <h3 class="green-text">Online</h3>
                            {{/if}}
                        {{/if}}
                    </div>
                </div>
            </div>
        </div>
    {{#grouped_each 2 xively}}
        <div class="row">
    {{#each this}}
            <div class="col s12 m6">
                <div class="card" id="xively-{{this/obj_name}}">
                    <div class="card-content">
                        <span class="card-title black-text">{{this/xively-friendly-name}}</span>
                        <h3 class="black-text" id="value">{{this/xively-output}}</h3>
                    </div>
                </div>
            </div>
        {{/each}}
        </div>
        {{/grouped_each}}
    {{#if issues}}
    <div class="row">
        <div class="col s12 m6 offset-m3">
            <div linkurl="{{issues/publink}}" id="issue-card" class="card tooltipped issue waves-effect waves-block waves-light" data-position="bottom" data-delay="100" data-tooltip="The current issues!">
                <span class="card-title black-text">Current Issues:</span>
                <h6 class="green-text">Open: {{issues/open_count}}</h6>
                <h6 class="red-text">Closed: {{issues/closed_count}}</h6>
            </div>
        </div>
    </div>
    {{/if}}